# 开发记录 v0.3.9（2026-02-25 18:28:47）

1. 本步 DoD
- 功能完成条件：`report-summary` 支持 `--include-stats-json` 输出运行统计字段。
- 验收方式：JSON 报告中每条 run 可携带 `stats_json`。
- 测试通过条件：新增 stats_json 回归通过 + 全量回归通过。
- 产物清单：`src/cli.py`、`tests/test_cli_regression.py`。
- 不做范围：不修改 runs 表结构与统计生成逻辑。

2. 假设与风险
- 假设：`runs.stats_json` 可能为空或非 JSON 字符串。
- 风险：解析失败导致报告命令报错。
- 处理：解析失败时回退原始字符串，不中断报告生成。

3. TODO（带路径）
- [修改|直接相关] `src/cli.py`：新增参数并扩展 recent runs 查询字段。
- [修改|直接相关] `tests/test_cli_regression.py`：新增 include-stats-json 用例。

4. 测试计划
- 正常路径：`smoke-run` 后生成 JSON 报告，验证 stats_json 字段存在且内容正确。
- 回归路径：全量 CLI 回归。

5. 实现与改动摘要
- `src/cli.py`
  - 新增 `--include-stats-json`。
  - runs 查询增加 `stats_json` 列。
  - JSON 输出按需附加 `stats_json`，支持安全解析。
  - Markdown 输出按需追加 `stats_json` 文本。
- `tests/test_cli_regression.py`
  - 新增 `test_report_summary_include_stats_json`。

6. 自检结果（DoD 对照）
- DoD 满足。

7. 接口契约（如有）
- 接口名称：`report-summary --include-stats-json`
- 输入：flag，可选。
- 返回：JSON/Markdown 输出新增或附加 `stats_json` 信息。

8. 测试结果
- `python -m unittest tests/test_cli_regression.py -v`
- 结果：23 passed。

9. 风险与下一步建议
- 下一步可加时间窗口过滤，便于只看最近一段运行记录。

10. Git 提交建议
- commit: `v0.3.9: add optional stats_json in report-summary`
- tag: `v0.3.9`
