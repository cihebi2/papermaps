# 开发记录 v0.4.5（2026-02-25 19:35:06）

1. 本步 DoD
- 功能完成条件：网页支持读取/保存 OpenAlex key 与 mailto。
- 验收方式：`GET/POST /api/settings/openalex` 可用，页面可填写并保存。
- 测试通过条件：设置回写 roundtrip 与非法 mailto 400 都通过。
- 产物清单：`src/web_server.py`、`tests/test_cli_regression.py`。
- 不做范围：本步不发起 DOI 检索。

2. 假设与风险
- 假设：本地部署场景下可明文展示 key（无多用户权限隔离）。
- 风险：请求体异常导致双重响应。
- 处理：JSON body 解析失败时返回 `None` 并立即短路。

3. TODO（带路径）
- [修改|直接相关] `src/web_server.py`：新增设置 GET/POST 路由与页面表单。
- [修改|直接相关] `tests/test_cli_regression.py`：新增设置接口回归。

4. 测试计划
- 正常路径：保存后读取一致。
- 异常路径：mailto 不含 `@` 返回 400。
- 回归：CLI 主测试 + 存储扩展测试。

5. 实现与改动摘要
- `web_server.py`
  - 新增 `/api/settings/openalex` GET/POST。
  - 增加请求体 JSON 校验。
  - 页面新增 OpenAlex settings 面板与保存按钮。
- `tests/test_cli_regression.py`
  - 新增 `test_web_openalex_settings_roundtrip`。
  - 新增 `test_web_openalex_settings_invalid_mailto_returns_400`。

6. 自检结果（DoD 对照）
- DoD 满足。

7. 接口契约（如有）
- `GET /api/settings/openalex` -> `{api_key, mailto}`。
- `POST /api/settings/openalex` 输入 `{api_key, mailto}`。
- 错误：`400`（invalid JSON / invalid mailto）。

8. 测试结果
- `python -m unittest tests/test_cli_regression.py -v` -> 30 passed。
- `python -m unittest tests/test_storage_extensions.py -v` -> 2 passed。

9. 风险与下一步建议
- 下一步提供 DOI 检索 API，优先单 DOI -> 再支持多 DOI。

10. Git 提交建议
- commit: `v0.4.5: add web openalex settings api and ui`
- tag: `v0.4.5`
