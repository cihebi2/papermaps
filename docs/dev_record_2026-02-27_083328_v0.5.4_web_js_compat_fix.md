# v0.5.4 开发记录（Web JS Compatibility Fix）

时间戳：2026-02-27 08:33:28
版本：v0.5.4

## 1. 本步 DoD
- 功能完成条件：网页点击无反应问题得到修复，至少在不支持 `?.`/`??`/`replaceAll` 的浏览器中页面脚本可正常解析并工作。
- 验收方式：打开 `http://127.0.0.1:8765/`，填写 DOI 点击 Search 能看到结果区更新。
- 测试通过条件：现有 Python 回归测试全通过。
- 产物清单：修改 `src/web_server.py`。
- 不做范围：不改后端 API，不新增依赖，不做整体前端重写。

## 2. 假设与风险
- 假设：用户“无反应”是由于浏览器不支持较新的 JS 语法（解析阶段失败导致脚本未执行）。
- 风险：仍可能存在更旧的浏览器不支持 `async/await` 等语法；若仍无反应，需要进一步降级实现。

## 3. TODO（带路径）
- `src/web_server.py`（修改，直接相关）：移除前端脚本中 `?.`/`??`/`replaceAll`。

## 4. 测试计划
- 回归：
  - `python -m unittest tests/test_storage_extensions.py -v`
  - `python -m unittest tests/test_cli_regression.py -v`

## 5. 实现与改动摘要
- `src/web_server.py`：
  - `esc()` 使用正则 `replace(/.../g, ...)` 替代 `replaceAll`。
  - 去掉 `counts[key] ?? 0`、`row.result_summary?.found ?? '-'`、`payload.seed?.paper_id` 等语法，改为兼容写法。

## 6. 自检结果（DoD 对照）
- 已移除三处高风险新语法点，降低脚本解析失败概率（满足）。
- 不影响后端与数据库（满足）。

## 7. 接口契约（如有）
- 无。

## 8. 测试结果
- `tests/test_storage_extensions.py`: 2 passed
- `tests/test_cli_regression.py`: 45 passed

## 9. 风险与下一步建议
- 若用户仍遇到“无反应”，优先检查浏览器是否支持 `async/await`；必要时下一步将前端网络层降级为 XHR + Promise-free。

## 10. Git 提交建议
- commit message：`v0.5.4: fix dashboard js compatibility`
- tag：`v0.5.4`
