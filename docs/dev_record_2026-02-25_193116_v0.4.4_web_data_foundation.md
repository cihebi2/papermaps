# 开发记录 v0.4.4（2026-02-25 19:31:16）

1. 本步 DoD
- 功能完成条件：为网页后续交互补齐基础数据层：应用设置、保存查询、通知目标、告警去重与推送状态。
- 验收方式：存储扩展测试通过，且现有 CLI 回归不受影响。
- 测试通过条件：`tests/test_storage_extensions.py` 与 `tests/test_cli_regression.py` 全通过。
- 产物清单：`src/db_init.py`、`src/storage.py`、`tests/test_storage_extensions.py`。
- 不做范围：本步不开放网页写接口，不改页面。

2. 假设与风险
- 假设：SQLite 足够承载当前阶段的设置与通知元数据。
- 风险：新索引/新列可能影响旧库迁移。
- 处理：全部使用 `CREATE TABLE IF NOT EXISTS` + `ALTER TABLE` 式幂等迁移。

3. TODO（带路径）
- [修改|直接相关] `src/db_init.py`：新增表与告警去重索引，补 alerts 扩展列。
- [修改|直接相关] `src/storage.py`：新增 settings/saved_search/notification/alert 相关存储函数。
- [新增|直接相关] `tests/test_storage_extensions.py`：覆盖新增存储功能。

4. 测试计划
- 正常路径：设置保存读取、保存查询写入读取、通知目标 upsert/list。
- 边界路径：告警重复写入去重，标记 pushed 状态。
- 回归：CLI 28 条回归确保无行为回退。

5. 实现与改动摘要
- `db_init.py`
  - 新增 `app_settings` / `saved_searches` / `notification_targets` 表。
  - alerts 增加 `payload_json`、`pushed_at`，并添加唯一索引 `idx_alerts_unique`。
- `storage.py`
  - 新增 `set_app_setting/get_app_setting/list_app_settings`。
  - 新增 `add_saved_search/list_saved_searches`。
  - 新增 `upsert_notification_target/list_notification_targets`。
  - 新增 `add_alert/list_alerts/mark_alert_pushed`。
- `tests/test_storage_extensions.py`
  - 新增 2 组扩展存储测试。

6. 自检结果（DoD 对照）
- DoD 全部满足。

7. 接口契约（如有）
- 本步新增存储层接口（内部函数），供后续网页 API 调用。

8. 测试结果
- `python -m unittest tests/test_storage_extensions.py -v` -> 2 passed。
- `python -m unittest tests/test_cli_regression.py -v` -> 28 passed。

9. 风险与下一步建议
- 下一步做网页设置接口：可在页面填写并保存 OpenAlex key/mailto。

10. Git 提交建议
- commit: `v0.4.4: add web data foundation tables and storage helpers`
- tag: `v0.4.4`
