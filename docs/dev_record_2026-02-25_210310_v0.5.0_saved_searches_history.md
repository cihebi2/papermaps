# 开发记录 v0.5.0（2026-02-25 21:03:10）

1. 本步 DoD
- 功能完成条件：检索结果可保存并提供历史读取接口，页面可查看历史列表。
- 验收方式：执行检索后 `GET /api/saved-searches` 返回新增记录。
- 测试通过条件：历史读取成功 + limit 非法 400。
- 产物清单：`src/web_server.py`、`tests/test_cli_regression.py`。
- 不做范围：本步不做自动扫描与推送。

2. 假设与风险
- 假设：历史快照以摘要字段足够满足查看诉求。
- 风险：历史数据持续增长。
- 处理：读取接口支持 `limit`，前端默认限制为 10。

3. TODO（带路径）
- [修改|直接相关] `src/web_server.py`：新增 saved searches API 与 UI。
- [修改|直接相关] `tests/test_cli_regression.py`：新增历史接口回归。

4. 测试计划
- 正常路径：batch resolve 后读取历史至少一条，含 DOI。
- 异常路径：`limit=0` 返回 400。
- 回归：CLI + storage 扩展测试。

5. 实现与改动摘要
- 新增 `GET /api/saved-searches?limit=`。
- 单 DOI、多 DOI 检索接口新增 `save_search` 逻辑。
- 页面新增 Saved Searches 面板及刷新按钮。

6. 自检结果（DoD 对照）
- DoD 满足。

7. 接口契约（如有）
- 接口名称：`GET /api/saved-searches`
- 输入：`limit`（>0）。
- 返回：`{items, limit}`，每项含 `doi_list`、`result_summary`、`created_at`。
- 错误：`400 limit must be > 0 / integer`。

8. 测试结果
- `python -m unittest tests/test_cli_regression.py -v` -> 39 passed。
- `python -m unittest tests/test_storage_extensions.py -v` -> 2 passed。

9. 风险与下一步建议
- 下一步加入“自动扫描最新引用论文”的服务端接口，形成自动追踪能力。

10. Git 提交建议
- commit: `v0.5.0: add saved-search history api and dashboard panel`
- tag: `v0.5.0`
