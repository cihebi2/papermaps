# 开发记录 v0.3.7（2026-02-25 18:24:47）

1. 本步 DoD
- 功能完成条件：新增 `remove-watch-target` 命令可删除指定 watch 目标。
- 验收方式：先新增目标，再删除，列表应为空。
- 测试通过条件：新增删除成功用例 + 全量回归通过。
- 产物清单：`src/storage.py`、`src/cli.py`、`tests/test_cli_regression.py`。
- 不做范围：不新增按 id 删除，不改表结构。

2. 假设与风险
- 假设：target-type + target-value 可唯一定位 watch 目标。
- 风险：paper 类型 target-value 非规范值导致误删失败。
- 处理：沿用 canonical 校验，非法值直接报错。

3. TODO（带路径）
- [修改|直接相关] `src/storage.py`：新增删除函数。
- [修改|直接相关] `src/cli.py`：新增命令与路由。
- [修改|直接相关] `tests/test_cli_regression.py`：新增删除回归测试。

4. 测试计划
- 正常路径：新增 -> 删除 -> 列表为空。
- 边界/异常：本轮不扩展（下一轮补 not found 语义）。

5. 实现与改动摘要
- 存储层新增 `remove_watch_target`，返回删除行数。
- CLI 新增 `remove-watch-target` 子命令。
- 回归新增 `test_remove_watch_target_success`。

6. 自检结果（DoD 对照）
- DoD 满足。

7. 接口契约（如有）
- 接口名称：`remove-watch-target`
- 输入：`--db-path --target-type --target-value`
- 返回：成功 0；目标不存在或参数非法返回非 0。

8. 测试结果
- `python -m unittest tests/test_cli_regression.py -v`
- 结果：21 passed。

9. 风险与下一步建议
- 下一步补“删除不存在目标”的失败语义测试，保证契约完整。

10. Git 提交建议
- commit: `v0.3.7: add remove-watch-target command`
- tag: `v0.3.7`
