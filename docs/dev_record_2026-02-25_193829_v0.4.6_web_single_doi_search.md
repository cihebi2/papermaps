# 开发记录 v0.4.6（2026-02-25 19:38:29）

1. 本步 DoD
- 功能完成条件：网页支持单 DOI 检索并保存（papers + 可选 watch target）。
- 验收方式：`POST /api/works/resolve-doi` 返回工作信息，`save_watch=true` 时写入 watch。
- 测试通过条件：成功路径与无效 DOI 400 都通过。
- 产物清单：`src/web_server.py`、`tests/test_cli_regression.py`。
- 不做范围：本步不支持多 DOI 批量、不做相关/相似扩展。

2. 假设与风险
- 假设：单 DOI 是最小交互入口，便于先打通链路。
- 风险：真实网络不稳定会影响用户体验。
- 处理：测试侧采用 mock client，避免依赖外网。

3. TODO（带路径）
- [修改|直接相关] `src/web_server.py`：新增 DOI 检索接口与页面表单。
- [修改|直接相关] `tests/test_cli_regression.py`：新增 resolve-doi 成功/失败用例。

4. 测试计划
- 正常路径：有效 DOI 返回 work，并保存 paper + watch。
- 异常路径：bad DOI 返回 400。
- 回归：CLI 与存储扩展测试全跑。

5. 实现与改动摘要
- `web_server.py`
  - 新增 `POST /api/works/resolve-doi`。
  - 新增 `_build_openalex_client`、`_normalize_doi`、`_serialize_work`。
  - 页面新增 DOI 输入与查询按钮。
- `tests/test_cli_regression.py`
  - 新增 `test_web_resolve_doi_saves_paper_and_watch_target`（mock OpenAlex）。
  - 新增 `test_web_resolve_doi_invalid_doi_returns_400`。

6. 自检结果（DoD 对照）
- DoD 满足。

7. 接口契约（如有）
- `POST /api/works/resolve-doi`
  - 输入：`{doi: str, save_watch: bool}`。
  - 成功：`200 {ok, saved_to_watch, work}`。
  - 错误：`400 invalid doi`、`404 work not found`。

8. 测试结果
- `python -m unittest tests/test_cli_regression.py -v` -> 32 passed。
- `python -m unittest tests/test_storage_extensions.py -v` -> 2 passed。

9. 风险与下一步建议
- 下一步扩展为多 DOI 批量检索，支持一次性查看多篇结果。

10. Git 提交建议
- commit: `v0.4.6: add web single-doi search and save flow`
- tag: `v0.4.6`
