# 开发记录 v0.4.7（2026-02-25 19:41:42）

1. 本步 DoD
- 功能完成条件：网页支持多 DOI 批量检索与保存。
- 验收方式：`POST /api/works/resolve-dois` 接收 DOI 数组，返回 found/failed。
- 测试通过条件：批量成功用例通过，回归无破坏。
- 产物清单：`src/web_server.py`、`tests/test_cli_regression.py`。
- 不做范围：本步不做相似主题扩展。

2. 假设与风险
- 假设：用户可用换行/逗号提交多个 DOI。
- 风险：请求中混合非法 DOI。
- 处理：后端仅处理合法 DOI，返回失败列表。

3. TODO（带路径）
- [修改|直接相关] `src/web_server.py`：新增 batch DOI 路由和前端批量输入区。
- [修改|直接相关] `tests/test_cli_regression.py`：新增批量 DOI 回归测试。

4. 测试计划
- 正常路径：2 个可命中 DOI + 1 个不存在 DOI，返回 found=2 failed=1。
- 数据持久：papers 与 watch_targets 应写入 2 条。

5. 实现与改动摘要
- 新增 `POST /api/works/resolve-dois`。
- 页面新增 Batch DOI 输入区与查询按钮。
- 回归新增 `test_web_resolve_dois_batch_supports_multiple`。

6. 自检结果（DoD 对照）
- DoD 满足。

7. 接口契约（如有）
- `POST /api/works/resolve-dois`
  - 输入：`{dois: string[], save_watch?: bool}`。
  - 输出：`{ok, requested, found, failed}`。
  - 错误：`400`（dois 非数组或无合法 DOI）。

8. 测试结果
- `python -m unittest tests/test_cli_regression.py -v` -> 33 passed。
- `python -m unittest tests/test_storage_extensions.py -v` -> 2 passed。

9. 风险与下一步建议
- 下一步增加“相似主题/相关推荐”接口，返回与 DOI 相关的扩展文献。

10. Git 提交建议
- commit: `v0.4.7: add web batch doi search endpoint and ui`
- tag: `v0.4.7`
