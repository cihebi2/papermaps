# 开发记录 v0.3.8（2026-02-25 18:26:19）

1. 本步 DoD
- 功能完成条件：`remove-watch-target` 对“目标不存在”返回失败，并有可识别错误信息。
- 验收方式：空库删除目标，返回非 0 且包含 `not found`。
- 测试通过条件：新增异常测试通过，回归全绿。
- 产物清单：`tests/test_cli_regression.py`。
- 不做范围：不变更命令参数与存储实现。

2. 假设与风险
- 假设：命令失败语义对调用方很重要。
- 风险：如果日志文案未来修改，字符串断言可能脆弱。
- 处理：后续可改为结构化错误码（当前保持最小改动）。

3. TODO（带路径）
- [修改|直接相关] `tests/test_cli_regression.py`：新增 not-found 失败路径测试。

4. 测试计划
- 异常路径：`init-db` 后直接删不存在目标，检查失败码与错误关键词。
- 正常路径：现有全量回归。

5. 实现与改动摘要
- 新增 `test_remove_watch_target_not_found_fails`。

6. 自检结果（DoD 对照）
- DoD 满足。

7. 接口契约（如有）
- 接口名称：`remove-watch-target`
- 错误处理补充：目标不存在返回非 0，并输出 not found 错误上下文。

8. 测试结果
- `python -m unittest tests/test_cli_regression.py -v`
- 结果：22 passed。

9. 风险与下一步建议
- 下一步可增强 `report-summary` 输出，支持可选携带 `stats_json`。

10. Git 提交建议
- commit: `v0.3.8: add not-found regression test for remove-watch-target`
- tag: `v0.3.8`
