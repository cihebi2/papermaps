# 开发记录 v0.4.9（2026-02-25 20:59:38）

1. 本步 DoD
- 功能完成条件：新增相似主题检索接口，返回 topics + similar works。
- 验收方式：`POST /api/works/similar` 返回 `seed/topics/similar`，可选保存。
- 测试通过条件：正常路径 + 非法 DOI 400 均通过。
- 产物清单：`src/web_server.py`、`tests/test_cli_regression.py`。
- 不做范围：本步不做自动扫描/推送。

2. 假设与风险
- 假设：OpenAlex concepts 足够用于语义主题 query。
- 风险：concept 缺失时无法生成 query。
- 处理：返回明确错误 `No concepts/topics found`。

3. TODO（带路径）
- [修改|直接相关] `src/web_server.py`：新增 Similar Themes UI 与 `/api/works/similar`。
- [修改|直接相关] `tests/test_cli_regression.py`：新增 similar 成功/失败回归。

4. 测试计划
- 正常路径：返回 3 topics + 2 similar，保存后写入 `similar` 边。
- 异常路径：bad DOI 返回 400。
- 回归：CLI + storage 扩展测试。

5. 实现与改动摘要
- 新增 `_extract_topics`，按 concepts score 提取 top topics。
- 新增 `POST /api/works/similar`，使用 `iter_works(search=query)` 获取相似文献。
- 保存模式下写入 papers/watch_targets/edges(relation='similar')。

6. 自检结果（DoD 对照）
- DoD 满足。

7. 接口契约（如有）
- 接口名称：`POST /api/works/similar`
- 输入：`doi`、`max_similar`、`save`。
- 成功：`200 {ok, seed, topics, similar, save}`。
- 错误：`400 invalid doi / invalid max_similar / no topics`，`404 seed not found`。

8. 测试结果
- `python -m unittest tests/test_cli_regression.py -v` -> 37 passed。
- `python -m unittest tests/test_storage_extensions.py -v` -> 2 passed。

9. 风险与下一步建议
- 下一步补“保存查询历史并网页查看”，方便多 DOI 结果回看。

10. Git 提交建议
- commit: `v0.4.9: add web similar-themes api and ui`
- tag: `v0.4.9`
