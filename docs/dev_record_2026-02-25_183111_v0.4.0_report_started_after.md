# 开发记录 v0.4.0（2026-02-25 18:31:11）

1. 本步 DoD
- 功能完成条件：`report-summary` 支持 `--started-after YYYY-MM-DD` 过滤运行记录。
- 验收方式：报告仅包含 started_at 在指定日期之后的 runs。
- 测试通过条件：新增时间过滤用例通过，回归全通过。
- 产物清单：`src/cli.py`、`tests/test_cli_regression.py`。
- 不做范围：不引入时区配置，不改 started_at 存储格式。

2. 假设与风险
- 假设：`started_at` 文本格式为可比较的 `YYYY-MM-DD HH:MM:SS`。
- 风险：非法日期输入会导致模糊行为。
- 处理：新增 `--started-after` 格式校验，非法直接失败。

3. TODO（带路径）
- [修改|直接相关] `src/cli.py`：新增参数、校验与 SQL 过滤条件。
- [修改|直接相关] `tests/test_cli_regression.py`：新增 started-after 过滤测试。

4. 测试计划
- 正常路径：插入旧/新两条 run，过滤后只保留新记录。
- 异常路径：本轮先做格式校验逻辑，不单独加 invalid 测试（下一轮补）。

5. 实现与改动摘要
- `src/cli.py`
  - 新增 `--started-after`。
  - 增加日期格式校验。
  - 在所有 run 查询分支中加入 started_at 条件。
  - 报告 payload 增加 `started_after` 字段，markdown 增加展示。
- `tests/test_cli_regression.py`
  - 新增 `test_report_summary_started_after_filter`。

6. 自检结果（DoD 对照）
- DoD 满足。

7. 接口契约（如有）
- 接口名称：`report-summary --started-after`
- 输入：`YYYY-MM-DD`。
- 成功语义：仅返回满足 started_at >= 日期的 run。
- 错误语义：格式非法返回非 0。

8. 测试结果
- `python -m unittest tests/test_cli_regression.py -v`
- 结果：24 passed。

9. 风险与下一步建议
- 下一步补 invalid date 的失败回归，锁定错误语义。

10. Git 提交建议
- commit: `v0.4.0: add started-after filter to report-summary`
- tag: `v0.4.0`
