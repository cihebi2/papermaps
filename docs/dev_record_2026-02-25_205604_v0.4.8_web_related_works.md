# 开发记录 v0.4.8（2026-02-25 20:56:04）

1. 本步 DoD
- 功能完成条件：新增相关文献接口，按 DOI 返回参考文献与引用文献。
- 验收方式：`POST /api/works/related` 返回 `seed/references/citing`。
- 测试通过条件：正常路径 + 非法 DOI 400 都通过。
- 产物清单：`src/web_server.py`、`tests/test_cli_regression.py`。
- 不做范围：本步不做相似主题推荐。

2. 假设与风险
- 假设：OpenAlex 的 `referenced_works` 与 `iter_citing_works` 可定义相关文献。
- 风险：接口耗时受外网影响。
- 处理：增加 `max_references/max_citing` 限制。

3. TODO（带路径）
- [修改|直接相关] `src/web_server.py`：新增 `/api/works/related` 与页面交互区。
- [修改|直接相关] `tests/test_cli_regression.py`：新增 related 成功/异常回归。

4. 测试计划
- 正常路径：返回 seed + 2 references + 1 citing，落库 edges。
- 异常路径：bad DOI 返回 400。
- 回归：CLI + storage 扩展测试。

5. 实现与改动摘要
- `web_server.py`
  - 新增 Related Papers UI 面板。
  - 新增 `POST /api/works/related`。
  - 保存模式下写入 papers、watch_targets、edges（references/cites）。
- `tests/test_cli_regression.py`
  - `test_web_related_works_returns_references_and_citing`
  - `test_web_related_works_invalid_doi_returns_400`

6. 自检结果（DoD 对照）
- DoD 满足。

7. 接口契约（如有）
- 接口名称：`POST /api/works/related`
- 输入：`doi`、`max_references`、`max_citing`、`save`。
- 成功：`200 {ok, seed, references, citing, save}`。
- 错误：`400 invalid doi / invalid numeric params`，`404 seed not found`。

8. 测试结果
- `python -m unittest tests/test_cli_regression.py -v` -> 35 passed。
- `python -m unittest tests/test_storage_extensions.py -v` -> 2 passed。

9. 风险与下一步建议
- 下一步增加“相似主题文献”接口，补齐语义相关推荐。

10. Git 提交建议
- commit: `v0.4.8: add related-works api and ui panel`
- tag: `v0.4.8`
