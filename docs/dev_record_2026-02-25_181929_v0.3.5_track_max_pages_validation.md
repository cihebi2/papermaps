# 开发记录 v0.3.5（2026-02-25 18:19:29）

1. 本步 DoD
- 功能完成条件：`track-citations` 对 `--max-pages-per-target` 做正整数校验。
- 验收方式：传入 `0` 时直接失败并输出参数错误。
- 测试通过条件：全量回归通过，新增异常路径测试通过。
- 产物清单：`src/cli.py`、`tests/test_cli_regression.py`。
- 不做范围：不改 API 拉取逻辑与分页默认值。

2. 假设与风险
- 假设：分页参数非法应在网络调用前拦截。
- 风险：参数校验不一致会导致 CLI 行为割裂。
- 缓解：与 `run-scheduler` 对齐同一约束。

3. TODO（带路径）
- [修改|直接相关] `src/cli.py`：在 `track_citations` 起始处校验 `max-pages-per-target`。
- [修改|直接相关] `tests/test_cli_regression.py`：新增非法分页参数失败测试。

4. 测试计划
- 正常路径：回归全通过。
- 异常路径：`track-citations --dry-run --target-id W... --max-pages-per-target 0` 期望失败。

5. 实现与改动摘要
- `src/cli.py`：新增参数检查与错误日志。
- `tests/test_cli_regression.py`：新增 `test_track_citations_invalid_max_pages_per_target_fails`。

6. 自检结果（DoD 对照）
- DoD 满足。

7. 接口契约（如有）
- 接口名称：`track-citations --max-pages-per-target`
- 约束：必须 > 0；非法值返回非 0。

8. 测试结果
- `python -m unittest tests/test_cli_regression.py -v`
- 结果：19 passed。

9. 风险与下一步建议
- 建议下一步增强 watch 查询筛选能力，便于大规模目标管理。

10. Git 提交建议
- commit: `v0.3.5: validate track-citations max-pages-per-target`
- tag: `v0.3.5`
