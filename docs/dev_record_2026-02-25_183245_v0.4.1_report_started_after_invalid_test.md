# 开发记录 v0.4.1（2026-02-25 18:32:45）

1. 本步 DoD
- 功能完成条件：`report-summary --started-after` 非法日期输入有稳定失败回归覆盖。
- 验收方式：输入 `2026-99-99` 返回非 0 且提示 invalid。
- 测试通过条件：新增异常测试通过，回归全通过。
- 产物清单：`tests/test_cli_regression.py`。
- 不做范围：不变更实现逻辑，仅补测试。

2. 假设与风险
- 假设：错误文案可用于当前断言。
- 风险：日志文案改动会触发测试更新。
- 处理：保持断言关键词而非全句匹配。

3. TODO（带路径）
- [修改|直接相关] `tests/test_cli_regression.py`：新增 started-after 非法值测试。

4. 测试计划
- 异常路径：`--started-after 2026-99-99` 期望失败。
- 正常路径：全量回归。

5. 实现与改动摘要
- 新增 `test_report_summary_started_after_invalid_date_fails`。

6. 自检结果（DoD 对照）
- DoD 满足。

7. 接口契约（如有）
- 无接口变更，补充错误语义验证。

8. 测试结果
- `python -m unittest tests/test_cli_regression.py -v`
- 结果：25 passed。

9. 风险与下一步建议
- 下一步增强 `export-graph` 使用体验（格式别名或输出行为）。

10. Git 提交建议
- commit: `v0.4.1: add invalid started-after regression test`
- tag: `v0.4.1`
