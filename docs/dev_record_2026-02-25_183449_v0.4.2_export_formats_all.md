# 开发记录 v0.4.2（2026-02-25 18:34:49）

1. 本步 DoD
- 功能完成条件：`export-graph --formats all` 可一次导出 json/gexf/html。
- 验收方式：运行后输出目录存在 3 种格式文件。
- 测试通过条件：新增导出别名回归通过，回归全通过。
- 产物清单：`src/cli.py`、`tests/test_cli_regression.py`。
- 不做范围：不新增导出格式，不改导出内容结构。

2. 假设与风险
- 假设：`all` 仅作为 `json,gexf,html` 别名。
- 风险：与用户自定义混合值（如 `all,json`）可能重复。
- 处理：集合去重，行为稳定。

3. TODO（带路径）
- [修改|直接相关] `src/cli.py`：解析 `--formats` 时支持 `all` 别名。
- [修改|直接相关] `tests/test_cli_regression.py`：新增导出三文件回归测试。

4. 测试计划
- 正常路径：空库执行 `export-graph --formats all`，检查三类文件各 1 个。
- 回归路径：全量 CLI 测试。

5. 实现与改动摘要
- `src/cli.py`：在格式集合中识别并展开 `all`。
- `tests/test_cli_regression.py`：新增 `test_export_graph_formats_all_writes_three_files`。

6. 自检结果（DoD 对照）
- DoD 满足。

7. 接口契约（如有）
- 接口名称：`export-graph --formats`
- 变更：新增 `all` 语义，等价于 `json,gexf,html`。

8. 测试结果
- `python -m unittest tests/test_cli_regression.py -v`
- 结果：26 passed。

9. 风险与下一步建议
- 建议下一步增加 `report-summary --print`（输出到 stdout）便于流水线直接消费。

10. Git 提交建议
- commit: `v0.4.2: support all alias for export-graph formats`
- tag: `v0.4.2`
