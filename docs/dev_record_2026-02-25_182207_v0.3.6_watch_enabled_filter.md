# 开发记录 v0.3.6（2026-02-25 18:22:07）

1. 本步 DoD
- 功能完成条件：`list-watch-targets` 支持 `--enabled {0,1}` 精确筛选。
- 验收方式：同库存在启用/停用目标时，传 `--enabled 0` 仅返回停用目标。
- 测试通过条件：新增筛选用例通过，回归全通过。
- 产物清单：`src/cli.py`、`src/storage.py`、`tests/test_cli_regression.py`。
- 不做范围：不改 watch 表结构、不改默认排序。

2. 假设与风险
- 假设：`enabled` 只有 0/1 语义。
- 风险：`include-disabled` 与 `--enabled` 同时传入的语义可能混淆。
- 处理：`--enabled` 优先作为精确过滤条件。

3. TODO（带路径）
- [修改|直接相关] `src/cli.py`：增加 `--enabled` 参数并传递到存储层。
- [修改|直接相关] `src/storage.py`：`list_watch_targets` 增加 `enabled` 过滤。
- [修改|直接相关] `tests/test_cli_regression.py`：新增 enabled 过滤回归测试。

4. 测试计划
- 正常路径：`list-watch-targets --format json` 行为不变。
- 边界路径：`--include-disabled --enabled 0` 仅返回停用数据。

5. 实现与改动摘要
- `src/cli.py`：新增参数 `--enabled`（0/1）。
- `src/storage.py`：SQL 先应用 `enabled` 精确过滤，否则沿用 `include_disabled` 逻辑。
- `tests/test_cli_regression.py`：新增 `test_list_watch_targets_enabled_filter`。

6. 自检结果（DoD 对照）
- DoD 满足。

7. 接口契约（如有）
- 接口名称：`list-watch-targets --enabled`
- 输入：0/1，可选；提供后按该值过滤。
- 返回：文本/JSON 输出结构不变，仅数据集变化。

8. 测试结果
- `python -m unittest tests/test_cli_regression.py -v`
- 结果：20 passed。

9. 风险与下一步建议
- 建议下一步补“删除 watch 目标”能力，完成管理闭环。

10. Git 提交建议
- commit: `v0.3.6: add enabled filter to list-watch-targets`
- tag: `v0.3.6`
