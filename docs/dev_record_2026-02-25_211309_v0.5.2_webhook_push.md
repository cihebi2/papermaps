# 开发记录 v0.5.2（2026-02-25 21:13:09）

1. 本步 DoD
- 功能完成条件：支持 webhook 通知配置与新告警推送。
- 验收方式：配置 webhook 后调用 `POST /api/alerts/push-new`，webhook 可收到 payload，alerts 标记为 pushed。
- 测试通过条件：webhook 配置 roundtrip + push 成功回归通过。
- 产物清单：`src/web_server.py`、`tests/test_cli_regression.py`。
- 不做范围：本步不做定时自动触发。

2. 假设与风险
- 假设：用户可提供可访问的 webhook URL。
- 风险：外部 webhook 不稳定导致投递失败。
- 处理：返回 pushed/failed 统计和错误摘要，告警仅在成功后标记 pushed。

3. TODO（带路径）
- [修改|直接相关] `src/web_server.py`：新增 webhook 配置与 push API。
- [修改|直接相关] `tests/test_cli_regression.py`：新增 webhook roundtrip 与 push 回归。

4. 测试计划
- 正常路径：本地 webhook server 收到 payload，alerts 状态变为 pushed。
- 边界路径：无效 webhook URL 保存时返回 400（由 URL 前缀校验保障）。
- 回归：全量 CLI + storage 扩展测试。

5. 实现与改动摘要
- 新增 `GET/POST /api/notifications/webhook`。
- 新增 `POST /api/alerts/push-new`。
- 新增内部 `_send_webhook` 与 `_push_new_alerts`。
- 页面新增 Notification Push 面板（保存 webhook、触发推送）。

6. 自检结果（DoD 对照）
- DoD 满足。

7. 接口契约（如有）
- `POST /api/notifications/webhook`
  - 输入：`{url, enabled}`。
  - 错误：`400`（url 非 http/https）。
- `POST /api/alerts/push-new`
  - 输入：`{limit}`。
  - 成功：`{ok, alerts_considered, pushed, failed, errors}`。

8. 测试结果
- `python -m unittest tests/test_cli_regression.py -v` -> 43 passed。
- `python -m unittest tests/test_storage_extensions.py -v` -> 2 passed。

9. 风险与下一步建议
- 下一步加入“自动定时扫描 + 自动推送”控制接口，形成闭环自动化。

10. Git 提交建议
- commit: `v0.5.2: add webhook notification config and push api`
- tag: `v0.5.2`
