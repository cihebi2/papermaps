# 开发记录 v0.3.4（2026-02-25 18:17:19）

1. 本步 DoD
- 功能完成条件：`run-scheduler` 对 `--max-pages-per-target` 做参数校验（必须 > 0）。
- 验收方式：传入 `--max-pages-per-target 0` 时命令返回非 0 并输出错误。
- 测试通过条件：正常路径回归通过 + 异常路径新用例通过。
- 产物清单：`src/cli.py`、`tests/test_cli_regression.py`。
- 明确不做范围：不调整抓取逻辑、不改默认分页值。

2. 假设与风险
- 假设：无效分页参数应在进入调度前失败。
- 风险：仅 scheduler 校验，track 子命令仍需独立校验。
- 兜底：下一步补 `track-citations` 同类校验。

3. TODO（带路径）
- [修改|直接相关] `src/cli.py`：增加 `run_scheduler` 参数合法性判断。
- [修改|直接相关] `tests/test_cli_regression.py`：新增无效分页参数失败用例。

4. 测试计划
- 正常路径：全量回归。
- 异常路径：`run-scheduler --max-pages-per-target 0` 期望失败并包含错误提示。

5. 实现与改动摘要
- `src/cli.py`：新增 `max-pages-per-target <= 0` 判错与日志。
- `tests/test_cli_regression.py`：新增 `test_run_scheduler_invalid_max_pages_per_target_fails`。

6. 自检结果（DoD 对照）
- DoD 各项满足。

7. 接口契约（如有）
- 接口名称：`run-scheduler --max-pages-per-target`
- 变更：输入约束明确为正整数，非法值直接失败返回。

8. 测试结果
- `python -m unittest tests/test_cli_regression.py -v`
- 结果：18 passed。

9. 风险与下一步建议
- 建议下一步对 `track-citations` 同步补充 `max-pages-per-target` 输入校验，避免接口行为不一致。

10. Git 提交建议
- commit: `v0.3.4: validate scheduler max-pages-per-target argument`
- tag: `v0.3.4`
