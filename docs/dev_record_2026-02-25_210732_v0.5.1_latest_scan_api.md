# 开发记录 v0.5.1（2026-02-25 21:07:32）

1. 本步 DoD
- 功能完成条件：实现最新引用扫描接口并生成去重告警。
- 验收方式：`POST /api/latest/scan` 返回扫描摘要，alerts 表新增记录。
- 测试通过条件：正常路径生成告警 + 参数异常 400。
- 产物清单：`src/storage.py`、`src/web_server.py`、`tests/test_cli_regression.py`。
- 不做范围：本步不做外部推送通道。

2. 假设与风险
- 假设：enabled 的 paper watch target 都参与最新引用扫描。
- 风险：重复扫描重复告警。
- 处理：`add_alert_if_new` 返回创建标记 + alerts 唯一索引保证去重。

3. TODO（带路径）
- [修改|直接相关] `src/storage.py`：新增 `add_alert_if_new`。
- [修改|直接相关] `src/web_server.py`：新增 `/api/latest/scan` 与页面触发。
- [修改|直接相关] `tests/test_cli_regression.py`：新增 latest scan 成功/失败回归。

4. 测试计划
- 正常路径：mock citing works 产生 1 条新 alert。
- 异常路径：`max_pages_per_target=0` 返回 400。
- 回归：全量 CLI + storage 扩展测试。

5. 实现与改动摘要
- `storage.py`
  - 新增 `add_alert_if_new`，返回 `(alert_id, created)`。
- `web_server.py`
  - 新增 Latest Scan UI 区域与按钮。
  - 新增 `POST /api/latest/scan`，按 watch target 扫描 citing works。
  - 扫描结果包含 `targets_scanned/citing_papers_processed/alerts_new`。
- `tests/test_cli_regression.py`
  - 新增 `test_web_latest_scan_creates_new_alerts`。
  - 新增 `test_web_latest_scan_invalid_max_pages_returns_400`。

6. 自检结果（DoD 对照）
- DoD 满足。

7. 接口契约（如有）
- 接口名称：`POST /api/latest/scan`
- 输入：`lookback_days`、`max_pages_per_target`。
- 成功：`200 {ok, targets_scanned, citing_papers_processed, alerts_new, ...}`。
- 错误：`400`（参数非法）。

8. 测试结果
- `python -m unittest tests/test_cli_regression.py -v` -> 41 passed。
- `python -m unittest tests/test_storage_extensions.py -v` -> 2 passed。

9. 风险与下一步建议
- 下一步接 webhook 推送，把 new alerts 主动推送到你配置的地址。

10. Git 提交建议
- commit: `v0.5.1: add latest-scan api and new alert generation`
- tag: `v0.5.1`
